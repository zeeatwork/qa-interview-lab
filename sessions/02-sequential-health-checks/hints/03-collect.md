# Hint 3

Create an empty results array before the loop. During each iteration, await the current check and push its resolved value into the results array. Return the results after the loop finishes.
