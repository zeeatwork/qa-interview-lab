/**
 * Run asynchronous health-check functions one at a time and collect their results.
 *
 * @param {Array<() => Promise<unknown>>} checks
 * @returns {Promise<Array<unknown>>}
 */
export async function runHealthChecks(checks) {
  // Write your solution here.
}
